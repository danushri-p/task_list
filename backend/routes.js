
import { Router } from "express";
import express from "express";
use (express.json());
route = express.Router();

data = {getForm,postForm}


route.get('Get-data',getForm);
route.post('post-data',postForm);

module.exports = data;

